// The preview renders the real widgets without booting the Medusa admin shell.
export const defineRouteConfig = (config: unknown) => config
export const defineWidgetConfig = (config: unknown) => config
